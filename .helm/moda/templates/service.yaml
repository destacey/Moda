apiVersion: v1
kind: Service
metadata:
  name: {{ include "moda.fullname" . }}
  labels:
    {{- include "moda.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "moda.selectorLabels" . | nindent 4 }}
