apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "moda.fullname" . }}-test-connection"
  labels:
    {{- include "moda.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "moda.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
